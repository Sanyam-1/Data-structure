#include<iostream>
#include<string.h>
int main(){
    char s[20],t[20];
    std::cout<<"Enter the expression:";
    std::cin>>s;
    int k = strlen(s);
    std::cout<<strlen(s);
    for(int i=0;i<k;i++){

    }
}